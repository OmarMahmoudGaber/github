local_sqlite:
  target: dev
  outputs:
    dev:
      type: sqlite
      # Path to the file created by etl.py
      database: 'my_data.db'
      schema: 'main'
      schemas_and_paths:
        main: 'my_data.db'
      threads: 1
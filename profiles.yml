my_sqlite_profile:
  target: dev
  outputs:
    dev:
      type: sqlite
      database: 'my_data.db' # Path to the file created in Step 2
      schema: 'main'
      schemas_and_paths:
        main: 'my_data.db'
      threads: 1
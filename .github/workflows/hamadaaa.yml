name: my first workflow | training 

on:
    push:
        branches: main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        #    - name: checkout
        #      uses: actions/checkout@v6.0.2
            - name: Upload a Build Artifact
              uses: actions/upload-artifact@v6.0.0

            - name: list files 
              run : ls -ltra

            - name: run shell script
              run: 
                |
                  chmod +x script.sh
                  ./script22.sh